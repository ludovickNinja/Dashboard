# Dashboard

Simple static dashboard for tracking:
- Pending orders
- Orders assigned at factory
- Orders in-house
- Weekly pieces sent per factory
- Growth trends over time

## Run locally

```bash
python3 -m http.server 8000
```

Open `http://localhost:8000`.

## Customize data

Edit the `dashboardData` object in `app.js`.
